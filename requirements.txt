# Hybrid Reasoning Core — runtime + dev deps
# Pinned conservatively. macOS arm64 + Python 3.11+.

# --- HTTP & MCP ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
mcp[cli]>=1.0,<2.0

# --- Tree-sitter (Python, JavaScript, TypeScript, C#, SQL) ---
tree-sitter>=0.21,<0.24
tree-sitter-languages>=1.10,<2.0   # bundled grammars (preferred path)
# Per-language fallbacks if the aggregate wheel misses anything:
tree-sitter-python>=0.21
tree-sitter-javascript>=0.21
tree-sitter-typescript>=0.21
tree-sitter-c-sharp>=0.21
tree-sitter-sql>=0.3

# --- Real Mamba SSM backbone ---
transformers>=4.39,<5.0
torch>=2.2,<3.0
accelerate>=0.27
safetensors>=0.4
sentencepiece>=0.1.99       # tokenizer for some Mamba checkpoints
einops>=0.7                  # required by mamba-ssm code paths

# --- Test harness ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23
pytest-timeout>=2.2
