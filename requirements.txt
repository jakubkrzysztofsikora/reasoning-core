# Hybrid Reasoning Core — runtime + dev deps
# Pinned conservatively. macOS arm64 + Python 3.11+.

# --- HTTP & MCP ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
mcp[cli]>=1.0,<2.0

# --- Tree-sitter (Python, JavaScript, TypeScript, C#, SQL) ---
# tree-sitter-languages aggregate has no Py 3.13 wheel — use per-language
# wheels which all have current Py 3.13 binaries. The grammars loader in
# src/grammars.py prefers the aggregate when available and falls back here.
tree-sitter>=0.25,<0.27         # ABI v15 — matches modern per-language wheels
tree-sitter-python>=0.23
tree-sitter-javascript>=0.23
tree-sitter-typescript>=0.23
tree-sitter-c-sharp>=0.23
tree-sitter-sql>=0.3
tree-sitter-markdown>=0.5.1
tree-sitter-json>=0.23
tree-sitter-yaml>=0.7
# Data / markup grammars — embedding-only scoring, no call graph.
# Each is wrapped in `except ImportError` in src/grammars.py so a missing
# wheel for one of these doesn't break the others.
tree-sitter-css>=0.20
tree-sitter-html>=0.20
tree-sitter-vue>=0.2
tree-sitter-dockerfile>=0.2
# tree-sitter-scss not consistently available on PyPI for Py 3.13; loader
# falls back gracefully. Add here if a wheel becomes available.

# --- Real Mamba SSM backbone ---
transformers>=4.39,<5.0
torch>=2.2,<3.0
accelerate>=0.27
safetensors>=0.4
sentencepiece>=0.1.99       # tokenizer for some Mamba checkpoints
einops>=0.7                  # required by mamba-ssm code paths

# --- Test harness ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23
pytest-timeout>=2.2
