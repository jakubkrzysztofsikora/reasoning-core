__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.eggs/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.DS_Store
*.log
.env
.env.local
hf_cache/
.cache/
node_modules/
.claude/scheduled_tasks.lock

# direnv local overrides — secrets + personal toggles, never committed.
.envrc.local
# direnv state cache, per-machine.
.direnv/

# eval artifacts — partial run state + judge relabel cache + calibrated runs
eval/runs/*.partial.jsonl
eval/datasets/*.judge.jsonl
eval/calibrated/
.gemini/settings.json
.vibe/config.toml

.claude/

# Per-machine Cato VPN CA bundle, built by direnv on demand.
ca-bundle.pem

# >>> reasoning-core >>>
.envrc.local
.claude/settings.local.json
.gemini/settings.json
.vibe/config.toml
.reasoning-core/
# <<< reasoning-core <<<
eval/calibrated/prm_corpus.jsonl
